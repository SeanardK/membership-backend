services:
  backend:
    build:
      context: .
      dockerfile: dockerfile
    container_name: seanard-backend
    restart: unless-stopped
    ports:
      - "${PORT:-3001}:${PORT:-3001}"
    env_file:
      - .env
    volumes:
      - ./public:/root/public
    networks:
      - default
